// assets

import { IconPackage } from '@tabler/icons-react';

// constant
const icons = {
  IconPackage
};

// ==============================|| EXTRA PAGES MENU ITEMS ||============================== //

const vas = {
  id: 'vas',
  title: 'VAS',
  type: 'group',
  children: [
    {
      id: 'vas',
      title: 'VAS',
      type: 'item',
      url: '/vas/vasmain',
      icon: icons.IconPackage
    }
  ]
};

export default vas;
